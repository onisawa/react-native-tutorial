import React from 'react';
import { Text } from 'react-native';

import text from '../constant/text.json';

const Button = () => {
  return (
    <Text>{text.buy_btn}</Text>
  );
};

export default Button;
